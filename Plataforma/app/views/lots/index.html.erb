<p id="notice"><%= notice %></p>

<h1>Lotes</h1>
<a class="btn-floating btn-large waves-effect waves-light green" <%= link_to '+', new_farm_lot_path %><i class="material-icons"></i></a>
<td><%= link_to 'Volver', farms_path %></td>

<table>
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Ubicación</th>
      <th>Cultivo</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @lots.each do |lot| %>
      <tr>
        <td><%= lot.name %></td>
        <td><%= lot.location %></td>
        <td><%= lot.farm.name %></td>
        <td><%= link_to 'Ver surcos', farm_lot_grooves_path(lot_id: lot.id) %> </td>
        <td><%= link_to 'Editar', edit_farm_lot_path(id: lot.id) %></td>

        <td><%= link_to 'Destruir', controller: "lots", method: "delete", data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>


